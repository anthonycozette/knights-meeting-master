<h1>
    Jeux de role grandeur nature a venir
</h1>

{% for evenement in evenements %}
<div class="row mt-3" id="even">
	<div class="col-8">
		<div class="text-center">
			<h2>
				{{ evenement.nom }}
			</h2>
		</div>
		<a href="{{ path('evenement-details', {'slug':evenement.slug}) }}">
                    <img src="{{ vich_uploader_asset(evenement, 'imageFile') }}" alt="" class="img-fluid"></a>
		<div class="mt-5" id="text">
			{{ evenement.description|personal_trunc|raw }}
		</div>
	</div>
	<div class="col-4 mt-5">
		<div class="detail-even">
			<div class="detail-date">
                <div>
                    <i class="far fa-calendar-alt"></i>
                </div>
                {{ evenement.dateDebut|date('d-m-Y') }}
                au
                {{ evenement.dateFin|date('d-m-Y') }}
		    </div>
            <div class="even-adresse">
                <div>
                    <div>
						<i class="fas fa-map-marked-alt"></i>
					</div>
                    {{ evenement.adresse }}
                </div>
                <div>
                    {{ evenement.codePostal }}
                    {{ evenement.ville }}
                </div>
            </div>
            <div class="even-prix">
                <div>
					<i class="far fa-money-bill-alt"></i>
				</div>
                {{ evenement.prix }}
                €
            </div>
	    </div>
    </div>
    <div class="ms-2 mb-4">
        <a class="btn btn-info " id="bouton-bleu" href="{{ path('evenement-details',{'slug': evenement.slug}) }}">
            detail
        </a>
    </div>
</div>
{% endfor %}
{# {% for evenement in evenements %}

    <div class="col-12 col-md">

        <div class="row mt-4" id="even">

            <div class="col-3">
                <a href="{{ path('evenement-details', {'slug':evenement.slug}) }}">
                    <img src="{{ vich_uploader_asset(evenement, 'imageFile')|imagine_filter('echelle') }}" alt="" class="img-fluid"></a>
                </div>

                <div class="col-6">
                    <h3 class="text-center">
                        {{ evenement.nom }}
                    </h3>
                    <div class="font-even">
                        {{ evenement.description|u.truncate(200, '...', false) }}
                    </div>
                </div>

                <div class="col-3 text-center" id="even-border">
                    <div class="even-date">
                        <div>
							<i class="far fa-calendar-alt"></i>
						</div>
                        {{ evenement.dateDebut|date('d-m-Y') }}
                        au
                        {{ evenement.dateFin|date('d-m-Y') }}
                    </div>
                    <div class="even-adresse">
                        <div>
                            <div>
								<i class="fas fa-map-marked-alt"></i>
							</div>
                            {{ evenement.adresse }}
                        </div>
                        <div>
                            {{ evenement.codePostal }}
                            {{ evenement.ville }}
                        </div>
                    </div>
                    <div class="even-prix">
                        <div>
							<i class="far fa-money-bill-alt"></i>
						</div>
                        {{ evenement.prix }}
                        €
                    </div>
                </div>
                <div class="bouton-detail">
                    <a href="{{ path('evenement-details',{'slug': evenement.slug}) }}" id="bouton-detail">
                        detail
                    </a>
                </div>
            </div>
        </div>
    {% endfor %}
     #}